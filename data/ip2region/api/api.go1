package api

import (
	"context"
	"github.com/zzy-rabbit/xtools/xerror"
	"github.com/zzy-rabbit/xtools/xplugin"
)

const (
	PluginName = "repository.data.ip2region"
)

type Config struct {
	Path string `json:"path"`
}

type IPlugin interface {
	xplugin.IPlugin
	Search(ctx context.Context, ip string) ([]string, xerror.IError)
}
